document.addEventListener("DOMContentLoaded", () => {
    const main_index = document.querySelector(".main_index");

    if (main_index)
    {
        main_index.classList.toggle("visible");
    }

});
